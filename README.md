# WK Clients Importer

Clients importer implemented using WinForms in .NET Framework 4.8.

Current Import formats are **CSV** and **JSON**.

A template with the required fields can also be generated.

Data is stored into a local JSON file.


![image](docs/wk-clients-importer-v1.0.png)
